#!/usr/bin/env bash
SPRINT_DIR="$HOME/Documents/work/ai-sprints"
FILE=$(ls "$SPRINT_DIR"/sprint-*.md 2>/dev/null | sort -t- -k2 -n | tail -1)
if [ -n "$FILE" ]; then
    NVIM_APPNAME="nvim-personal" nvim "$FILE"
fi
